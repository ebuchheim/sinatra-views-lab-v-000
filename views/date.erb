<!DOCTYPE html>
<html>
    <head>
        <title>Date</title>
    </head>
    <body>
        <% t = Time.now %>
        <p><%= "The date is #{t.strftime("%A")}, #{t.strftime("%B")} #{t.day}, #{t.year}" %></p>
    </body>
</html>